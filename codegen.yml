overwrite: true
schema: src/schema/**/*.gql
silent: true
generates:
  src/types/generated/graphql.d.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      maybeValue: T | undefined
hooks:
  afterAllFileWrite:
    - yarn run lint:fix
