schema: './src/schema/**/*.gql'
generates:
  ./src/schema/resolvers.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      mappers:
        Capsule: ./src/contracts/api/capsule#Capsule
